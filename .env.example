# ===================================
# External Access Configuration
# ===================================

# External port for accessing the application
# The application will be available at http://localhost:${EXTERNAL_PORT}/simple-docking
EXTERNAL_PORT=8090

# Application base path (change if you want different URL structure)
APP_BASE_PATH=/simple-docking

# ===================================
# Database Configuration
# ===================================

# PostgreSQL settings
POSTGRES_DB=docking
POSTGRES_USER=docking
POSTGRES_PASSWORD=docking_password_change_me
POSTGRES_PORT=5432

# Database URL (auto-constructed from above)
DATABASE_URL=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:${POSTGRES_PORT}/${POSTGRES_DB}

# ===================================
# Redis/Valkey Configuration
# ===================================

REDIS_PORT=6379
BROKER_URL=redis://broker:${REDIS_PORT}/0

# ===================================
# Backend API Configuration
# ===================================

# Internal API port (usually no need to change)
API_PORT=8000

# File paths
OBJECT_STORE_PATH=/data/object_store
PROTEIN_LIBRARY_PATH=/protein_library

# Task settings
TASK_TIMEOUT_SECONDS=300
MAX_RETRIES=2

# Docking settings
POCKET_METHOD_DEFAULT=auto
POCKET_PADDING=6.0
POCKET_MIN_SIZE=18.0
POCKET_DEFAULT_SIZE=20.0

# ===================================
# Frontend Configuration
# ===================================

# Internal frontend port (usually no need to change)
FRONTEND_PORT=3000

# API base path for frontend (relative to APP_BASE_PATH)
VITE_API_BASE=${APP_BASE_PATH}/api

# ===================================
# Security Settings (Production)
# ===================================

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:${EXTERNAL_PORT},http://localhost:3000

# Rate limiting
RATE_LIMIT_PER_MINUTE=60

# ===================================
# Development Settings
# ===================================

# To expose DB and Redis ports for development,
# uncomment the respective 'ports' section in docker-compose.yml

# Development database port (optional)
# DEV_DB_PORT=5432

# Development Redis port (optional)
# DEV_REDIS_PORT=6379
